# RI-YOLO: Модуляция признаков, мотивированная теорией Retinex, и метрика Вассерштейна для детекции объектов в условиях экстремально низкой освещенности

<div align="center">

[![Python](https://img.shields.io/badge/Python-3.10%2B-blue.svg?logo=python&logoColor=white)](https://www.python.org/)
[![PyTorch](https://img.shields.io/badge/PyTorch-2.0%2B-ee4c2c.svg?logo=pytorch&logoColor=white)](https://pytorch.org/)
[![Ultralytics](https://img.shields.io/badge/Ultralytics-YOLOv8s-00599C.svg)](https://github.com/ultralytics/ultralytics)
[![Benchmark](https://img.shields.io/badge/Dataset-ExDark-success.svg)](https://github.com/cs-chan/Exclusively-Dark-Image-Dataset)
[![Roadmap](https://img.shields.io/badge/Roadmap-23%20Controlled%20Runs-purple.svg)](EXPERIMENT_JOURNAL.md)
[![License](https://img.shields.io/badge/License-AGPL--3.0-orange.svg)](https://www.gnu.org/licenses/agpl-3.0.en.html)

**Официальный репозиторий проекта RI-YOLO (Retinex-Informed YOLO)**  
*Модуляция признаков, мотивированная теорией Retinex, в латентном пространстве детектора без попиксельной реконструкции кадров (корреляция карты с яркостью составляет -0.019; в Фазе 1 регуляризатор RSL был деактивирован).*

</div>

---

<div align="center">
  <img src="figs/architecture.png" width="92%" alt="RI-YOLO Architecture">
</div>

---

## 📌 Аннотация и научная мотивация

Обнаружение объектов в условиях экстремально низкой освещенности (ночное наблюдение, беспилотный транспорт, автономные роботы) сталкивается с **«парадоксом улучшения» (Enhancement Paradox)**:

1. **Недостатки попиксельного улучшения (LLIE-моделей)**:  
   Классические методы предварительного осветления изображений (RetinexNet, Zero-DCE, KinD, EnlightenGAN) восстанавливают пиксели, но неизбежно **усиливают сенсорный шум матрицы, размывают контрастные границы объектов и порождают цветовые артефакты**. Кроме того, они создают неприемлемую вычислительную задержку (>40–60 мс), делая обработку в реальном времени невозможной.
2. **Ограничения стандартных детекторов на темных сценах**:  
   Прямое обучение сверточных сетей (таких как YOLOv8s) на низкоконтрастных темных кадрах приводит к деградации признаков из-за низкого соотношения сигнал/шум (SNR). Стандартная метрика IoU / CIoU становится нестабильной: малейшее смещение рамки малоконтрастного объекта на 1–2 пикселя приводит к обнулению перекрытия.

### 💡 Концепция RI-YOLO

**RI-YOLO (Retinex-Informed YOLO)** полностью исключает шаг попиксельного восстановления кадра и переносит принципы декомпозиции Retinex непосредственно в глубокое латентное пространство нейросети:

- **Spatial Retinex Feature Decoupling (RFDBlock)**: Выполняет декомпозицию признаков в backbone детектора на уровне $P_5$ (после SPPF). Модуль оценивает пространственную карту освещенности $L$ и выделяет структуру объектов $S$, масштабируемую обучаемым гейтом $\gamma$.
- **Retinex Smoothness Loss (RSL)**: Накладывает регуляризацию по полной вариации (TV-loss) на карту $L$, обеспечивая кусочно-гладкое распределение.
- **Normalized Wasserstein Distance (NWD)**: Моделирует рамки в виде 2D-гауссиан с физически откалиброванной по датасету константой масштаба, обеспечивая непрерывный градиент даже при нулевом пересечении IoU.

---

## 🗺️ Дорожная карта проекта (Project Roadmap)

Программа исследований строго регламентирована и включает **23 контролируемых GPU-запуска**, разделенных на 4 последовательные фазы:

```
[x] ФАЗА 0: Инфраструктурный аудит и калибровка констант      [100% ЗАВЕРШЕНО]
[x] ФАЗА 1: Структурная аблация и мультисид (Статья C1)        [100% ЗАВЕРШЕНО]
[x] ФАЗА 2: Функции потерь и калибровка метрики (Статья C2)   [100% ЗАВЕРШЕНО]
[>] ФАЗА 3: Системная синергия и режим обучения (Статья C3)   [E4' завершена (2 прогона), E5' не запускалась]
```

### ✅ Фаза 0: Инфраструктура, аудит и калибровка (Завершена)
- [x] **Математический тест тождественности при инициализации (CP-0.4)**: Подтверждено точное попиксельное совпадение выходов базового YOLOv8s и всех 5 модификаций RFD при $\gamma = 0$ ($|d| = 0.00\text{e+}00$, перенесено 96.2% тензоров из `yolov8s.pt`).
- [x] **Аппаратный бенчмарк сложности (CP-0.7)**: На видеокарте NVIDIA RTX 3070 Ti задержка базовой модели составляет $4.92$ мс против $5.15$ мс у RFD post-SPPF (накладные расходы всего **$+0.23$ мс**, или **$+4.6\%$**). При BS=16 FP16 пропускная способность превышает **$493$ FPS**.
- [x] **Калибровка метрики рамок NWD (CP-0.6)**: На основе анализа 9,731 боксов ExDark определен средний размер объекта ($163.89$ px) и зафиксированы физически обоснованные константы: $C_{rec} = 10.24$ (привязка к P4-stride) и $\tau_{size} = 5.12$.
- [x] **Семантическое открытие о природе карты Retinex (CP-0.5)**: Корреляция Пирсона карты $L$ с физической яркостью кадров $Y$ близка к нулю ($\rho = -0.0188$), при высокой пространственной вариативности ($L_{std} = 0.3853$). Это доказывает, что модуль обучается пространственному вниманию к информативным контурам объектов, а не физическому осветлению фона.
- [x] **Аудит обученных чекпойнтов (CP-0.3)**: Зафиксирована устойчивая динамика обучаемого гейта $\gamma \in [+0.002, +0.018]$ без риска вырождения.

### ✅ Фаза 1: Структурная аблация и мультисид-валидация (Статья C1 / РИНЦ) — [100% ЗАВЕРШЕНО]
- [x] **E3′: Исследование глубины встраивания** (5 запусков, 100 эпох, seed 0) — *Завершено, сформирована Таблица 1*:
  - `pos-p3`: слой 5, малое рецептивное поле TRF = 95 px (покрытие кадра 14.8%).
  - `pos-p4`: слой 7, промежуточный масштаб TRF = 239 px (покрытие 37.3%).
  - `pos-presppf`: слой 9, высокоуровневые признаки TRF = 399 px (до пулинга).
  - `pos-postsppf`: слой 10, глобальный контекст TRF = 783 px (покрытие 122.3%).
  - `capctrl`: контроль емкости (`RFDBlockNoGate`, 2.6M параметров, гейт 0.5, TRF = 783 px, покрытие 122.3%).
- [x] **E2′: Мультисид-валидация устойчивости** (5 запусков, 100 эпох, seeds 1, 2, 3) — *Завершено*:
  - Строгость локализации mAP@50-95: Welch t-test $t = 5.44, p = 0.0057 < 0.01$; по mAP@50 $t = 3.08, p = 0.096$ (уровня статистической значимости не достигает).

### ✅ Фаза 2: Функции потерь и варианты метрики (Статья C2 / ВАК) — [100% ЗАВЕРШЕНО]
- [x] **E6′: Сетка коэффициента регуляризации $\lambda_{tv}$** (4 запуска, 100 эпох) — *Завершено: зафиксирована область нечувствительности к весу TV-loss, различия по сетке в пределах 1.7 сигмы*.
- [x] **E7′: Формулировка метрики рамок NWD** (3 запуска, 100 эпох, $\lambda_{tv} = 0.01$) — *Завершено*:
  - `abs` ($C=10.24$): **mAP@50 = 0.6976**, **mAP@50-95 = 0.4376** (получено на seed 0; требует мультисид-валидации и изоляции от RSL).
  - `scaleinv` ($C=0.3$): mAP@50 = 0.6935, mAP@50-95 = 0.4367.
  - `sizegate` ($\tau=5.12$): дефект формы тензоров (FIX-1.4), требует повторного запуска.

### 🔄 Фаза 3: Системная синергия и режим обучения (Статья C3 / Scopus Q1)
- [x] **E4′: Влияние мозаичной аугментации** (2 запуска, 100 эпох):
  - Режимы `close_mosaic = 0` (0.6903 / 0.4350) и `close_mosaic = 50` (0.6929 / 0.4323) показали инвариантность к мозаике (размах 0.34 п.п. при сигме 0.26 п.п.).
- [ ] **E5′: Длительность обучения и эффективность выборки** (4 запуска, ожидает выполнения):
  - Сравнение базовой модели и RI-YOLO на 40 и 60 эпохах для построения кривой эффективности выборки.

---

## 🔬 Математический аппарат метода

### 1. Модуль Spatial Retinex Feature Decoupling (RFDBlock)

Для тензора признаков $x \in \mathbb{R}^{B \times C \times H \times W}$ на выходе SPPF выполняется адаптивная декомпозиция:

$$
\begin{aligned}
f &= \mathrm{Conv}_{1\times1}(x) \\
L(u, v) &= \sigma(\mathrm{Conv}_{1\times1}(f)) \quad \in [0, 1]^{B \times 1 \times H \times W} \\
S &= \mathrm{Conv}_{3\times3}(f \odot (1.0 - L)) \\
y &= x + \gamma \cdot S
\end{aligned}
$$

- $\gamma \in \mathbb{R}$ — обучаемый скаляр с инициализацией $\gamma = 0$ (тождественный старт).
- $L$ выполняет функцию селективного подавления затемненных и зашумленных областей.
- $S$ выделяет высокочастотные структурные границы объектов.

### 2. Функция потерь Retinex Smoothness Loss (RSL)

Для предотвращения резких скачков карты освещенности накладывается регуляризатор Total Variation (TV-loss) с нормировкой на число пространственных элементов:

$$
\mathcal{L}_{\text{RSL}} = \frac{1}{B \cdot H \cdot W} \sum_{b=1}^{B} \left( \sum_{h, w} |L_{b,1,h+1,w} - L_{b,1,h,w}| + \sum_{h, w} |L_{b,1,h,w+1} - L_{b,1,h,w}| \right)
$$

Итоговая функция потерь детектора:

$$
\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{det}} + \lambda_{\text{tv}} \cdot \mathcal{L}_{\text{RSL}}, \quad \text{где } \lambda_{\text{tv}} = 0.001
$$

### 3. Метрика рамок Normalized Wasserstein Distance (NWD)

Ограничивающие рамки аппроксимируются двумерными гауссианами $\mathcal{N}(\mu, \Sigma)$. Расстояние Вассерштейна 2-го порядка выражается аналитически:

$$
W_2^2(\mathcal{N}_1, \mathcal{N}_2) = \|\mu_1 - \mu_2\|_2^2 + \frac{(w_1 - w_2)^2 + (h_1 - h_2)^2}{4}
$$

Нормализованная метрика:

$$
\mathrm{NWD} = \exp\left( -\frac{\sqrt{W_2^2 + \epsilon^2}}{C_{rec}} \right)
$$

- $C_{rec} = 10.24$ — константа масштаба, физически привязанная к шагу признаков P4-stride датасета ExDark.
- В режиме **`sizegate`** вес метрики плавно затухает для крупных объектов:  
  $\alpha(s) = \alpha_0 \cdot \exp\left(-\frac{s}{\tau_{size}}\right)$, где $s = \sqrt{w \cdot h}$, $\alpha_0 = 0.2$, $\tau_{size} = 5.12$.

---

## 📊 Текущие экспериментальные результаты

> **Важное замечание о воспроизводимости**:  
> На датасете **ExDark** (12 классов, экстремальная темнота) стандартный предобученный детектор **YOLOv8s** при 100 эпохах обучения достигает честного уровня **$\approx 0.696$ mAP@50**. Все сравнения в данном проекте проводятся относительно этого строгого бейслайна в равных условиях без спекулятивных занижений.

### Аппаратный профиль на NVIDIA GeForce RTX 3070 Ti (8 GB VRAM)

Данные получены скриптом `scripts/bench.py` на зафиксированном стенде (PyTorch 2.5.1, CUDA 12.1):

| Архитектура | Параметры | GFLOPs | Режим (BS, Prec) | Задержка p50 (мс) | Задержка p95 (мс) | Пропускная способность |
|:---|:---:|:---:|:---:|:---:|:---:|:---:|
| **YOLOv8s (Baseline)** | 11.17M | 28.81 | BS=1, FP32 | **4.92 мс** | 6.93 мс | 203.2 FPS |
| **YOLOv8s (Baseline)** | 11.17M | 28.81 | BS=16, FP16 | **32.91 мс** | 34.19 мс | 486.2 FPS |
| **RI-YOLO (pos-postsppf)** | 13.79M | 30.91 | BS=1, FP32 | **5.15 мс** | 6.12 мс | 194.4 FPS |
| **RI-YOLO (pos-postsppf)** | 13.79M | 30.91 | BS=16, FP16 | **32.41 мс** | 32.65 мс | **493.7 FPS** |
| **RI-YOLO (capctrl)** | 13.79M | 30.91 | BS=16, FP16 | **32.38 мс** | 32.62 мс | **494.2 FPS** |

*Накладные расходы модуля RFD составляют всего $+0.23$ мс (+4.6%), сохраняя частоту кадров выше 190 FPS при одиночных запросах и свыше 490 FPS в пакетном режиме.*

### Результаты Фазы 1: Аблация глубины встраивания и мультисид-валидация (Статья C1)

Результаты 100 эпох обучения на датасете ExDark (10 запусков: серии E3′ и E2′). Артефакты сформированы скриптом `scripts/collect_c1.py` (`artifacts/phase1/summary.csv`, `artifacts/phase1/C1_table1.csv`):

| Архитектура / Конфигурация | Запуски ($N$) | TRF (px) | Покрытие кадра | $\Delta$ Параметры | mAP@50 ($\text{mean} \pm \sigma$) | mAP@50-95 ($\text{mean} \pm \sigma$) | $\gamma_{final}$ | $\Delta$ mAP@50 (п.п.) |
|:---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| **YOLOv8s (Baseline)** | 2 | — | — | 0.00M | $0.6888 \pm 0.0028$ | $0.4300 \pm 0.0006$ | — | 0.00 |
| **RI-YOLO (pos-p3)** | 1 | 95.0 | 14.8% | +0.16M | **0.6965** | 0.4345 | -0.000546 | **+0.77** |
| **RI-YOLO (pos-p4)** | 1 | 239.0 | 37.3% | +0.66M | 0.6920 | 0.4332 | +0.019775 | +0.32 |
| **RI-YOLO (pos-presppf)** | 1 | 399.0 | 62.3% | +2.62M | 0.6933 | 0.4326 | +0.026108 | +0.45 |
| **RI-YOLO (pos-postsppf)** | **4** | **783.0** | **122.3%** | +2.62M | **$0.6962 \pm 0.0026$** | **$0.4348 \pm 0.0016$** | +0.003897 | **+0.74** |
| **RI-YOLO (capctrl)** | 1 | **783.0** | **122.3%** | +2.62M | 0.6924 | 0.4370 | +0.008713 | +0.36 |

**Ключевые фактические выводы Фазы 1:**
1. **Строгость локализации (mAP@50-95)**:  
   Мультисид-сравнение 4 прогонов post-SPPF против бейслайна показало прирост **$+0.48$ п.п.** Двусторонний **Welch t-test: $t = 5.44, p = 0.0057 < 0.01$**. Прирост по mAP@50 составляет $+0.74$ п.п. при $t = 3.08, p = 0.096$ (уровня статистической значимости $p < 0.05$ не достигает).
2. **Сравнение с контролем емкости (`capctrl`)**:  
   Контроль емкости (`RFDBlockNoGate`, TRF = 783 px, 2.6M параметров) демонстрирует mAP@50-95 = 0.4370, не уступая полному блоку (0.4348).
3. **Градиентная динамика гейта $\gamma$**:  
   На слое P3 модуль практически обнуляется ($\gamma \approx 0$). На глубоких слоях (P4, pre-SPPF, post-SPPF) гейт стабильно положителен ($\gamma > 0$), однако метрические различия между всеми 4 позициями находятся в пределах межсидового шума ($\sigma = 0.26$ п.п.).

### Результаты Фазы 2: Сетка веса регуляризатора $\lambda_{tv}$ (Серия E6′)

Результаты 100 эпох обучения на датасете ExDark (модель `yolov8s-rfd-postsppf`, seed 0). Артефакт сформирован скриптом `scripts/collect_c2.py` (`artifacts/phase2/C2_table1.csv`):

| Конфигурация | $\lambda_{tv}$ | mAP@50 | mAP@50-95 | $\gamma_{final}$ | Интерпретация |
|:---|:---:|:---:|:---:|:---:|:---|
| `e6p__lam-1e-4__s0` | $10^{-4}$ (0.0001) | 0.6937 | 0.4349 | +0.002043 | Вариации в пределах стохастического шума |
| `e6p__lam-1e-3__s0` | $10^{-3}$ (0.001) | 0.6938 | 0.4303 | +0.000282 | Промежуточный баланс |
| **`e6p__lam-1e-2__s0`** | **$10^{-2}$ (0.01)** | **0.6960** | **0.4355** | +0.002626 | **Значение в области нечувствительности: различия по сетке не превышают 1,7 стандартного отклонения** |
| `e6p__lam-1e-1__s0` | $10^{-1}$ (0.1) | 0.6916 | 0.4329 | +0.014175 | Намечается деградация, уровня значимости не достигает |

**Фактический вывод**: Значения mAP50 варьируются в диапазоне $0.6938 \pm 0.0026$ (размах $0.44$ п.п. при межзапусковом стандартном отклонении $0.26$ п.п., то есть $1.7\sigma$). Оптимум функции потерь экспериментально не доказан; значение $\lambda_{tv} = 0.01$ зафиксировано как рабочая точка внутри зоны нечувствительности.

---

## ⚠️ Известные ограничения (Known Limitations)

1. **Качество детекции инвариантно к глубине встраивания модуля**: Различия между позициями P3, P4, pre-SPPF и post-SPPF лежат в пределах межсидового шума обучения ($\sigma = 0.26$ п.п., ранговая корреляция с TRF составляет $-0.05$).
2. **Вычислительная сложность инвариантна к уровню пирамиды**: Для всех уровней добавленная сложность составляет $\approx 2.1$ GFLOPs (сумма 30.91 против 28.81 GFLOPs), тогда как число параметров различается в 16 раз (+0.16M у P3 против +2.62M у post-SPPF).
3. **Контроль емкости не уступает полному модулю по строгости локализации**: Архитектура `capctrl` (`RFDBlockNoGate` с фиксированным весом 0.5) достигает mAP@50-95 = 0.4370 против 0.4348 у полного RFDBlock с обучаемым гейтом и оценкой $L$.
4. **Прирост по mAP@50 статистически не значим**: При сравнении 4 прогонов post-SPPF с бейслайном прирост по mAP@50 составляет $+0.74$ п.п. при $p = 0.096 > 0.05$. Статистическая значимость доказана только по метрике строгости локализации mAP@50-95 ($p = 0.0057 < 0.01$).

---

## 🛠️ Установка и быстрый старт

### 1. Клонирование и виртуальное окружение

```bash
git clone https://github.com/Mr-Flyguy/ri-yolo.git
cd ri-yolo

# Создание виртуального окружения
python -m venv .venv

# Активация окружения (Windows PowerShell)
.\.venv\Scripts\Activate.ps1
# Или Linux / macOS:
# source .venv/bin/activate

# Установка пакета в режиме разработки
pip install -e .
```

### 2. Подготовка датасета ExDark

Официальное разбиение ExDark: 3000 обучающих, 1800 валидационных, отложенная тестовая часть 2563 изображения не использовалась.

Структура каталога `datasets/ExDark`:

```text
datasets/
└── ExDark/
    ├── images/
    │   ├── train/          # 3000 обучающих изображений
    │   └── val/            # 1800 валидационных изображений
    └── labels/
        ├── train/          # 9731 объект (плотность 3.24 об/кадр)
        └── val/            # 5704 объекта (плотность 3.17 об/кадр)
```

Конфигурационный файл `exdark.yaml` находится в корне репозитория.

---

## 🚀 Воспроизведение экспериментов

Все эксперименты запускаются через единый стандартизированный CLI-оркестратор:

```bash
# Запуск всей серии E3' (исследование глубины встраивания, 5 моделей по 100 эпох):
python scripts/train_phase1.py --run e3 --data exdark.yaml --device 0 --epochs 100

# Запуск отдельной конфигурации (например, канонический post-SPPF):
python scripts/train_phase1.py --run e3p__pos-postsppf__s0 --data exdark.yaml --device 0 --epochs 100

# Аппаратный бенчмарк инференса на локальном GPU:
python scripts/bench.py --batches 1 16 --half 0 1 --out artifacts/phase0/bench_local.csv

# Анализ пространственной корреляции карты Retinex с яркостью:
python scripts/corr_LY.py --ckpt runs/e3p__pos-postsppf__s0/weights/best.pt --images exdark.yaml
```

---

## 📚 Цитирование

Если вы используете архитектуру RI-YOLO, калибровки констант или инструментарий в своих исследованиях, пожалуйста, используйте следующее библиографическое описание:

```bibtex
@article{riyolo2026,
  title={RI-YOLO: Retinex-Informed Latent Feature Decoupling and Normalized Wasserstein Distance for Robust Low-Light Object Detection},
  author={RI-YOLO Research Team},
  journal={Препринт / Статья в разработке},
  year={2026}
}
```

---

## 📄 Лицензия

Кодовая база распространяется под лицензией [GNU Affero General Public License v3.0 (AGPL-3.0)](https://www.gnu.org/licenses/agpl-3.0.en.html) в соответствии со стандартами Ultralytics.
